## Funzzy events file
# more details see: https://github.com/cristianoliveira/funzzy
#
# list here all the events and the commands that it should execute

- name: quick checks
  run: 
    - cargo test
  change: 
    - Cargo.toml
    - src/**
    - tests/**

- name: lint & formatters
  run: 
    - cargo fmt
  change: 
    - Cargo.toml
    - src/**
    - tests/**

- name: nix flake 
  run: 
    - nix build .#pono
    - nix build .#ponoNigthly
  change: 
    - Cargo.toml
    - src/**
    - tests/**
    - "*.nix"
  run_on_init: true

- name: stage files in git
  run: 
    - git add {{relative_path}}
    - git add -p
  change: 
    - Cargo.toml
    - "*.md"
    - src/**
    - tests/**
    - "*.nix"
